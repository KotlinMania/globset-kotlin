# Port targets — `globset-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `globset` that codex-rs uses, and therefore the minimum subset that `globset-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-58] (rank `56`, tier `direct`, status `To Do`)

**Cargo package name**: `globset`
**Rust module name (in `use` statements)**: `globset`

## Summary

- **4** distinct symbols imported by codex-rs
- **9** total `use` statements
- **4** codex source files affected
- **3** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `globset::GlobBuilder`

- **3 `use` site(s)** across **3 codex member crate(s)**: `codex-linux-sandbox`, `codex-network-proxy`, `codex-protocol`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `linux-sandbox/src/bwrap.rs:36` (`codex-linux-sandbox`) — `use globset::GlobBuilder`
- `network-proxy/src/policy.rs:7` (`codex-network-proxy`) — `use globset::GlobBuilder`
- `protocol/src/permissions.rs:9` (`codex-protocol`) — `use globset::GlobBuilder`

### `globset::GlobSet`

- **3 `use` site(s)** across **2 codex member crate(s)**: `codex-linux-sandbox`, `codex-network-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `linux-sandbox/src/bwrap.rs:37` (`codex-linux-sandbox`) — `use globset::GlobSet`
- `network-proxy/src/policy.rs:8` (`codex-network-proxy`) — `use globset::GlobSet`
- `network-proxy/src/runtime.rs:22` (`codex-network-proxy`) — `use globset::GlobSet`

### `globset::GlobSetBuilder`

- **2 `use` site(s)** across **2 codex member crate(s)**: `codex-linux-sandbox`, `codex-network-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `linux-sandbox/src/bwrap.rs:38` (`codex-linux-sandbox`) — `use globset::GlobSetBuilder`
- `network-proxy/src/policy.rs:9` (`codex-network-proxy`) — `use globset::GlobSetBuilder`

### `globset::GlobMatcher`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-protocol`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `protocol/src/permissions.rs:10` (`codex-protocol`) — `use globset::GlobMatcher`
