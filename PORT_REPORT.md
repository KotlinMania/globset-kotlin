# Port report — `globset-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `globset`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 20 `use` sites from 4 other repos
- **Outbound**: 7 `use` sites to 1 other repos
- **Unresolved external**: 3 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `globset` | `globset` | 20 | 1 | COD-58 | 56 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (20 edges, 6 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `ignore-kotlin` | `ignore` | 7 |  |
| `codex-kotlin` | `codex-network-proxy` | 4 | ✅ |
| `codex-kotlin` | `codex-linux-sandbox` | 3 | ✅ |
| `codex-kotlin` | `codex-protocol` | 2 | ✅ |
| `insta-kotlin` | `insta` | 2 |  |
| `starlark-kotlin` | `starlark_bin` | 2 |  |

### Symbol-level demand (6 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `globset::GlobBuilder` | 6 | 3 | 3 | 5 |
| `globset::GlobSet` | 5 | 3 | 2 | 3 |
| `globset::GlobSetBuilder` | 4 | 2 | 2 | 3 |
| `globset::GlobMatcher` | 3 | 1 | 2 | 3 |
| `globset::Candidate` | 1 | 0 | 1 | 1 |
| `globset::Glob` | 1 | 0 | 1 | 1 |

#### Per-symbol detail (top 6 symbols)

##### `globset::GlobBuilder` (6 sites, 3 codex, 3 other, 5 caller crates)

Codex call sites:

- `codex-kotlin/codex-linux-sandbox` `src/bwrap.rs:36` — `use globset::GlobBuilder`
- `codex-kotlin/codex-network-proxy` `src/policy.rs:7` — `use globset::GlobBuilder`
- `codex-kotlin/codex-protocol` `src/permissions.rs:9` — `use globset::GlobBuilder`

Other workspace call sites:

- `ignore-kotlin/ignore` `src/gitignore.rs:9` — `use globset::GlobBuilder`
- `ignore-kotlin/ignore` `src/types.rs:4` — `use globset::GlobBuilder`
- `insta-kotlin/insta` `src/glob.rs:4` — `use globset::GlobBuilder`

##### `globset::GlobSet` (5 sites, 3 codex, 2 other, 3 caller crates)

Codex call sites:

- `codex-kotlin/codex-linux-sandbox` `src/bwrap.rs:37` — `use globset::GlobSet`
- `codex-kotlin/codex-network-proxy` `src/policy.rs:8` — `use globset::GlobSet`
- `codex-kotlin/codex-network-proxy` `src/runtime.rs:22` — `use globset::GlobSet`

Other workspace call sites:

- `ignore-kotlin/ignore` `src/gitignore.rs:9` — `use globset::GlobSet`
- `ignore-kotlin/ignore` `src/types.rs:4` — `use globset::GlobSet`

##### `globset::GlobSetBuilder` (4 sites, 2 codex, 2 other, 3 caller crates)

Codex call sites:

- `codex-kotlin/codex-linux-sandbox` `src/bwrap.rs:38` — `use globset::GlobSetBuilder`
- `codex-kotlin/codex-network-proxy` `src/policy.rs:9` — `use globset::GlobSetBuilder`

Other workspace call sites:

- `ignore-kotlin/ignore` `src/gitignore.rs:9` — `use globset::GlobSetBuilder`
- `ignore-kotlin/ignore` `src/types.rs:4` — `use globset::GlobSetBuilder`

##### `globset::GlobMatcher` (3 sites, 1 codex, 2 other, 3 caller crates)

Codex call sites:

- `codex-kotlin/codex-protocol` `src/permissions.rs:10` — `use globset::GlobMatcher`

Other workspace call sites:

- `insta-kotlin/insta` `src/glob.rs:4` — `use globset::GlobMatcher`
- `starlark-kotlin/starlark_bin` `bin/suppression.rs:6` — `use globset::GlobMatcher`

##### `globset::Candidate` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `ignore-kotlin/ignore` `src/gitignore.rs:9` — `use globset::Candidate`

##### `globset::Glob` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `starlark-kotlin/starlark_bin` `bin/suppression.rs:4` — `use globset::Glob`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `serde-kotlin` | 7 | 7 | `serde` | COD-128 | 1 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `regex-automata` | 62 | 26 | `globset` |
| `bstr` | 12 | 4 | `globset` |
| `aho-corasick` | 1 | 1 | `globset` |
