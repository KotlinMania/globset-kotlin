# Rust callers — `globset-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `globset` | `globset` | 20 | 6 | 4 | 9 |

## `globset`

### Inbound symbols (6 distinct, 20 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `globset`.

#### `globset::GlobBuilder` — 6 site(s) (3 codex, 3 other, 5 caller crates)

- ✅ `codex-kotlin/codex-linux-sandbox` `src/bwrap.rs:36` — `use globset::GlobBuilder`
- ✅ `codex-kotlin/codex-network-proxy` `src/policy.rs:7` — `use globset::GlobBuilder`
- ✅ `codex-kotlin/codex-protocol` `src/permissions.rs:9` — `use globset::GlobBuilder`
- `ignore-kotlin/ignore` `src/gitignore.rs:9` — `use globset::GlobBuilder`
- `ignore-kotlin/ignore` `src/types.rs:4` — `use globset::GlobBuilder`
- `insta-kotlin/insta` `src/glob.rs:4` — `use globset::GlobBuilder`

#### `globset::GlobSet` — 5 site(s) (3 codex, 2 other, 3 caller crates)

- ✅ `codex-kotlin/codex-linux-sandbox` `src/bwrap.rs:37` — `use globset::GlobSet`
- ✅ `codex-kotlin/codex-network-proxy` `src/policy.rs:8` — `use globset::GlobSet`
- ✅ `codex-kotlin/codex-network-proxy` `src/runtime.rs:22` — `use globset::GlobSet`
- `ignore-kotlin/ignore` `src/gitignore.rs:9` — `use globset::GlobSet`
- `ignore-kotlin/ignore` `src/types.rs:4` — `use globset::GlobSet`

#### `globset::GlobSetBuilder` — 4 site(s) (2 codex, 2 other, 3 caller crates)

- ✅ `codex-kotlin/codex-linux-sandbox` `src/bwrap.rs:38` — `use globset::GlobSetBuilder`
- ✅ `codex-kotlin/codex-network-proxy` `src/policy.rs:9` — `use globset::GlobSetBuilder`
- `ignore-kotlin/ignore` `src/gitignore.rs:9` — `use globset::GlobSetBuilder`
- `ignore-kotlin/ignore` `src/types.rs:4` — `use globset::GlobSetBuilder`

#### `globset::GlobMatcher` — 3 site(s) (1 codex, 2 other, 3 caller crates)

- ✅ `codex-kotlin/codex-protocol` `src/permissions.rs:10` — `use globset::GlobMatcher`
- `insta-kotlin/insta` `src/glob.rs:4` — `use globset::GlobMatcher`
- `starlark-kotlin/starlark_bin` `bin/suppression.rs:6` — `use globset::GlobMatcher`

#### `globset::Candidate` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `ignore-kotlin/ignore` `src/gitignore.rs:9` — `use globset::Candidate`

#### `globset::Glob` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `starlark-kotlin/starlark_bin` `bin/suppression.rs:4` — `use globset::Glob`
